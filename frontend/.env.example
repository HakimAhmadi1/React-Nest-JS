# Base URL of the API, including the /api prefix.
# Required — the app throws at startup rather than silently building
# "undefined/..." request URLs.
VITE_API_URL=http://localhost:3003/api

# Shown in the browser tab and as a fallback before settings load.
VITE_APP_NAME="Full-Stack Boilerplate"
